<div id="thProductLightbox"
     class="th-product-lightbox"
     hidden
     aria-hidden="true">

    <div class="th-product-lightbox-backdrop"
         data-lightbox-close></div>

    <div class="th-product-lightbox-dialog"
         role="dialog"
         aria-modal="true"
         aria-label="Product image preview">

        <button type="button"
                class="th-product-lightbox-close"
                data-lightbox-close
                aria-label="Close image preview">
            ×
        </button>

        <img id="thProductLightboxImage"
             src=""
             alt=""
             class="th-product-lightbox-image" />

        <div id="thProductLightboxCaption"
             class="th-product-lightbox-caption"></div>

    </div>

</div>

<style>
    .th-product-image-interactive {
        position: relative;
    }

    .th-image-zoom-trigger {
        width: 100%;
        height: 100%;
        border: none;
        padding: 0;
        margin: 0;
        background: transparent;
        cursor: zoom-in;
        display: block;
    }

    .th-product-image-interactive .th-product-photo,
    .th-product-image-interactive img {
        width: 100%;
        height: 100%;
        object-fit: contain;
        display: block;
        padding: 10px;
        box-sizing: border-box;
    }

    .th-image-detail-overlay {
        position: absolute;
        left: 0;
        right: 0;
        bottom: 0;
        padding: 28px 14px 14px;
        background: linear-gradient(
            to top,
            rgba(20, 16, 30, 0.92) 0%,
            rgba(20, 16, 30, 0.62) 72%,
            transparent 100%);
        color: #ffffff;
        pointer-events: none;
        z-index: 2;
    }

    .th-overlay-title {
        font-size: 13px;
        font-weight: 900;
        line-height: 1.3;
        margin-bottom: 4px;
    }

    .th-overlay-meta {
        font-size: 11px;
        opacity: 0.92;
    }

    .th-overlay-seller {
        font-weight: 800;
    }

    .th-overlay-price {
        font-size: 16px;
        font-weight: 900;
        margin-top: 6px;
    }

    .th-overlay-description {
        font-size: 11px;
        line-height: 1.45;
        margin-top: 5px;
        opacity: 0.95;
        max-height: 2.9em;
        overflow: hidden;
    }

    .th-product-image-interactive .status-badge {
        z-index: 4;
    }

    .th-product-lightbox {
        position: fixed;
        inset: 0;
        z-index: 2000;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
    }

    .th-product-lightbox[hidden] {
        display: none !important;
    }

    .th-product-lightbox-backdrop {
        position: absolute;
        inset: 0;
        background: rgba(20, 16, 30, 0.88);
    }

    .th-product-lightbox-dialog {
        position: relative;
        z-index: 1;
        width: min(96vw, 920px);
        max-height: 92vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 12px;
    }

    .th-product-lightbox-close {
        position: absolute;
        top: -8px;
        right: 0;
        width: 42px;
        height: 42px;
        border: none;
        border-radius: 50%;
        background: rgba(255, 255, 255, 0.95);
        color: #252238;
        font-size: 28px;
        line-height: 1;
        cursor: pointer;
    }

    .th-product-lightbox-image {
        width: 100%;
        max-height: calc(92vh - 70px);
        object-fit: contain;
        border-radius: 16px;
        background: #ffffff;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.35);
    }

    .th-product-lightbox-caption {
        color: #ffffff;
        font-size: 14px;
        font-weight: 700;
        text-align: center;
        max-width: 100%;
    }
</style>

<script>
    (function () {

        const lightbox =
            document.getElementById("thProductLightbox");

        if (!lightbox) {
            return;
        }

        const lightboxImage =
            document.getElementById("thProductLightboxImage");

        const lightboxCaption =
            document.getElementById("thProductLightboxCaption");

        function openLightbox(imageUrl, title) {

            if (!imageUrl) {
                return;
            }

            lightboxImage.src = imageUrl;
            lightboxImage.alt = title || "Product image";
            lightboxCaption.textContent = title || "";
            lightbox.hidden = false;
            lightbox.setAttribute("aria-hidden", "false");
            document.body.style.overflow = "hidden";
        }

        function closeLightbox() {

            lightbox.hidden = true;
            lightbox.setAttribute("aria-hidden", "true");
            lightboxImage.src = "";
            lightboxCaption.textContent = "";
            document.body.style.overflow = "";
        }

        document.addEventListener("click", function (event) {

            const trigger =
                event.target.closest(".th-image-zoom-trigger");

            if (trigger) {
                event.preventDefault();
                event.stopPropagation();

                openLightbox(
                    trigger.dataset.image,
                    trigger.dataset.title);

                return;
            }

            if (
                event.target.closest("[data-lightbox-close]") ||
                event.target === lightbox
            ) {
                closeLightbox();
            }
        });

        document.addEventListener("keydown", function (event) {

            if (
                event.key === "Escape" &&
                !lightbox.hidden
            ) {
                closeLightbox();
            }
        });

    })();
</script>
